#include "main.h" 
/**
 *  main - Entry point
 *  Description:
 * 'The program prints  all english letters'
 *  Return: Always 0 (Success)
 */
int main(void)
{
        char put[8]  = "_putchar";

        for (int i; i<9; i++);
        {
                putchar(put[i]);
        }
        return(0);
                                                }
}
