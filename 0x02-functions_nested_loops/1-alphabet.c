#include <stdio.h>
#include <stdlib.h>
/**
 *  main - Entry point
 *  Description:
 * 'The program prints  all english letters'
 *  Return: Always 0 (Success)
 */
int main(void)
{
	 char put[8]  = "_putchar";

	 for (char ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}				        }
	putchar('\n');
	return (0);
}
