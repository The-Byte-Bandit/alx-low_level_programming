#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

for(char i = 'a'; i<='z'; i ++)
{
if(i == e || i == q){
continue;
}else (){
	putchar(i);
}
}
putchar('\n');

return (0);
}

